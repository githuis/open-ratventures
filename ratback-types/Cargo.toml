[package]
name = "ratback-types"
version = "1.0.0"
edition = "2024"

[features]
sqlx = ["dep:sqlx"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"

[dependencies.sqlx]
version = "0.8"
features = ["sqlite"]
optional = true
